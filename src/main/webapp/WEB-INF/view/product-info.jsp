<!DOCTYPE html>

<html>
    <body>
        <h1>Info about ${product.title}</h1>
            ${product.id} <br>
            ${product.title} <br>
            ${product.cost} <br>
    </body>
    <br>
    <a href="${pageContext.request.contextPath}/">to Index Page</a>
</html>